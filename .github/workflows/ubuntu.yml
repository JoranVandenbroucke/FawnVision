name: Linux - Build & Test

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    name: Linux - Build & Test
    runs-on: ubuntu-latest

    strategy:
      fail-fast: false

      matrix:
        build_type: [ Debug, Release ]
        std: [ 23 ]
        cc: [ clang-18 ]
        cxx: [ clang++-18 ]
    steps:
      - uses: actions/checkout@v4
      - name: Prepare environment
        run: |
          sudo apt-get update
          sudo apt-get install -y unzip wget
          sudo wget https://github.com/ninja-build/ninja/releases/download/v1.12.1/ninja-linux.zip
          sudo unzip ninja-linux.zip
          sudo mv ninja /usr/local/bin/ninja
          wget https://apt.llvm.org/llvm.sh
          chmod +x llvm.sh
          sudo ./llvm.sh 18 all
          sudo apt-get install -y clang-tools-18  # Ensure clang-scan-deps is installed

      - name: Download Vulkan SDK
        run: |
          wget -qO- https://sdk.lunarg.com/sdk/download/1.3.275.0/linux/vulkan_sdk.tar.gz | tar -xz -C ${{runner.workspace}}/VulkanSDK

      - name: Copy Vulkan SDK folders
        run: |
          mkdir -p ${{runner.workspace}}/3rdParty/Vulkan
          cp -r ${{runner.workspace}}/VulkanSDK/1.3.275.0/x86_64/include ${{runner.workspace}}/3rdParty/Vulkan/include
          cp -r ${{runner.workspace}}/VulkanSDK/1.3.275.0/x86_64/lib ${{runner.workspace}}/3rdParty/Vulkan/libraries
          cp -r ${{runner.workspace}}/VulkanSDK/1.3.275.0/x86_64/bin ${{runner.workspace}}/3rdParty/Vulkan/binaries

      - name: Configure CMake
        env:
          CC: ${{matrix.cc}}
          CXX: ${{matrix.cxx}}
        run: >
          cmake -B ${{runner.workspace}}/Build
          -G Ninja
          -DCMAKE_BUILD_TYPE=${{ matrix.build_type }}
          -DCMAKE_C_COMPILER=${{ matrix.cc}}
          -DCMAKE_CXX_COMPILER=${{ matrix.cxx}}
          -DCMAKE_CXX_STANDARD=${{matrix.std}}
          -DCMAKE_CXX_COMPILER_SCAN_DEPS="/usr/lib/llvm-18/bin/clang-scan-deps"
          -S ${{ github.workspace }}

      - name: Build
        run: cmake --build ${{runner.workspace}}/Build --config ${{ matrix.build_type }}
